databases:
  - name: feeloom-postgres
    plan: free

services:
  - type: web
    name: feeloom-web
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput
    startCommand: python manage.py migrate && python manage.py bootstrap_admin && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT
    healthCheckPath: /health/
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: feeloom-postgres
          property: connectionString
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DJANGO_DEBUG
        value: false
      - key: FEELOOM_PUBLIC_HOSTNAME
        value: feeloom.app
      - key: FEELOOM_TOKEN_ENCRYPTION_KEY
        generateValue: true
      - key: EMAIL_HOST
        value: smtp-relay.brevo.com
      - key: EMAIL_PORT
        value: 2525
      - key: EMAIL_USE_TLS
        value: true
      - key: EMAIL_USE_SSL
        value: false
      - key: EMAIL_HOST_USER
        sync: false
      - key: EMAIL_HOST_PASSWORD
        sync: false
      - key: DEFAULT_FROM_EMAIL
        sync: false
      - key: FEELOOM_SUPPORT_EMAIL
        sync: false
      - key: GOOGLE_ANALYTICS_MEASUREMENT_ID
        sync: false
      - key: FEELOOM_BACKUP_READY
        value: false
      - key: FEELOOM_BOOTSTRAP_ADMIN_ON_START
        sync: false
      - key: FEELOOM_BOOTSTRAP_ADMIN_USERNAME
        sync: false
      - key: FEELOOM_BOOTSTRAP_ADMIN_EMAIL
        sync: false
      - key: FEELOOM_BOOTSTRAP_ADMIN_PASSWORD
        sync: false
      - key: ETSY_API_KEY
        sync: false
      - key: ETSY_SHARED_SECRET
        sync: false
